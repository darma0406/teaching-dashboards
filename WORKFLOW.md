# 教學駕駛艙製作、優化與發布流程

這份文件是日後製作 Codex skill 的詳細流程規格。目標是讓 Codex 在使用者提供教材、單元或製作需求後，能穩定產出可在 GitHub Pages 公開使用的教學駕駛艙。

核心原則：

- 原始教材、PDF、PPT、影片、NotebookLM 原始檔保留在本機教材工作區。
- GitHub 只放公開網站真正需要的靜態檔案。
- 每個駕駛艙都要能讓學生、老師直接公開使用。
- 網站要有總首頁、科目首頁、單元頁三層結構。
- 單元頁要能回到所屬科目首頁。
- 學生可能用手機或平板閱讀，因此響應式設計是必要條件。

## Skill 觸發情境

未來這個流程做成 skill 時，應在以下情境觸發：

- 使用者要把某個教材單元做成「教學駕駛艙」。
- 使用者要把 NotebookLM、教材 PDF、教冊、簡報、影片整理成互動網頁。
- 使用者要新增一個駕駛艙到 `darma0406/teaching-dashboards`。
- 使用者要檢查駕駛艙是否符合 GitHub Pages 限制。
- 使用者要把駕駛艙部署到 GitHub Pages。
- 使用者要把已完成的駕駛艙整理成 GitHub 上傳版。
- 使用者要維護總首頁、科目首頁或單元入口。

## 預期輸入

製作前盡量取得以下資訊；缺少時可先用合理預設，但要在回覆中標明假設：

- 科目：例如國小數學、國語、自然、社會。
- 年級與學期：例如五年級下學期。
- 版本或出版社：例如康軒。
- 單元名稱與單元序號：例如第七單元 表面積。
- 教材來源檔案：PDF、教冊、習作、簡報、圖片。
- NotebookLM notebook 或輸出素材。
- 影片連結或影片來源。
- 預期使用情境：教室大屏、觸控螢幕、學生手機、學生平板。
- 是否要立即部署到 GitHub Pages。

## 預期輸出

一次完整工作應產出：

- 原始工作資料夾內的單元駕駛艙草稿。
- `GitHub上傳版/` 中的精簡可上傳版本。
- 總首頁、科目首頁、單元頁入口。
- 單元頁的回科目首頁按鈕。
- GitHub Pages 可公開使用的網址。
- 一份簡短的檢查結果：大小限制、響應式、功能、部署狀態。

## 目錄與命名規則

### 原始教材工作區

原始教材工作區保留完整素材，不要求符合 GitHub Pages 上傳限制。

範例：

```text
G:\我的雲端硬碟\03_教學資料\02_114教材\
  康軒五下第七單元 表面積\
    index.html
    style.css
    script.js
    assets/
    preview-*.png
    NotebookLM-影片下載說明.txt
```

### GitHub 上傳版

`GitHub上傳版/` 是唯一準備推送到 GitHub 的資料夾。

範例：

```text
GitHub上傳版/
  index.html
  site.css
  README.md
  WORKFLOW.md
  .nojekyll
  國小數學/
    index.html
  康軒五下第七單元-表面積/
    index.html
    style.css
    script.js
    assets/
```

### 單元資料夾命名

使用可讀、穩定、適合 URL 的資料夾名稱。

格式：

```text
出版社年級學期第N單元-單元名稱
```

範例：

```text
康軒五下第七單元-表面積
```

注意：

- 可保留中文，GitHub Pages 可以使用中文 URL。
- 用 `-` 連接單元名稱，避免空白。
- 原始工作資料夾可以用空白，例如 `康軒五下第七單元 表面積`。
- 上傳版建議避免空白，例如 `康軒五下第七單元-表面積`。

## 第 1 階段：讀取教材與建立教學骨架

這一階段的目標是先理解教材，不要急著做網頁。

### 1.1 確認教材來源

先掃描並列出可用素材：

- 課本 PDF。
- 教師手冊 PDF。
- 習作或習作教冊 PDF。
- 備課簡報。
- NotebookLM 產出的簡報、資訊圖、摘要。
- YouTube 或 NotebookLM 影片總覽。
- 使用者補充的教學目標或課堂需求。

不要移動、刪除、改名原始教材，除非使用者明確要求。

### 1.2 Codex 讀取教材

Codex 要先閱讀教材，整理：

- 單元核心概念。
- 本單元學生需要會的知識與技能。
- 3-5 個適合做成駕駛艙分頁或焦點的教學重點。
- 常見迷思。
- 適合互動化的圖像或操作。
- 形成性評量可問的基本題。

本次表面積案例的教學重點是：

- 認識表面積。
- 正方體表面積。
- 長方體表面積。
- 切開後的表面積變化。
- 堆疊後的表面積變化。

### 1.3 Claude 也要讀取教材

工作流程第一階段需讓 Claude 也讀取教材內容，以利後續製作。

目的：

- 讓 Claude 提供另一份教材理解。
- 協助檢查 Codex 整理出的教學重點是否遺漏。
- 協助後續產生文字、簡報提示、活動描述、評量題。

執行原則：

- Claude 使用同一份教材或同一組教材摘錄。
- Claude 的輸出要回到本流程整合，不直接覆蓋 Codex 判斷。
- 若 Codex 與 Claude 理解不一致，列出差異，請使用者或教材內容裁決。

### 1.4 產出教學骨架

骨架至少包含：

- 單元標題。
- 年級、學期、版本。
- 教學重點清單。
- 每個重點的學習目標。
- 每個重點的互動活動。
- 每個重點的 1-3 題形成性評量方向。
- 所需素材清單。

## 第 2 階段：生成與整理素材

### 2.1 NotebookLM 素材

可使用 NotebookLM 產生：

- 單元摘要。
- 教學簡報。
- 資訊圖表。
- 影片總覽。
- 學生自學提示。

限制：

- NotebookLM 有時會拒絕產生圖片，例如回傳 `Could not create infographic`。
- NotebookLM 影片下載可能需要登入，工具直接下載可能失敗。
- 失敗時要保留說明檔，不要假裝已下載成功。

### 2.2 影片處理原則

影片不要放 GitHub Pages。

標準做法：

1. 影片上傳 YouTube。
2. 設定為公開或不公開。
3. 在駕駛艙中使用 YouTube embed。

範例：

```html
<iframe src="https://www.youtube.com/embed/VIDEO_ID"></iframe>
```

### 2.3 圖像處理原則

可放入 GitHub 的素材：

- PNG。
- JPG。
- SVG。
- WebP。
- HTML/CSS/JS。

不放入 GitHub 上傳版的素材：

- PDF。
- PPT/PPTX。
- MP4/MOV/AVI/MKV/WebM。
- NotebookLM 原始檔。
- 測試圖。
- 預覽截圖。
- 中間檢查圖。

### 2.4 NotebookLM 失敗時的替代方案

若 NotebookLM 無法產生資訊圖：

- 用 SVG 做簡化視覺圖。
- 用 HTML/CSS/JS 直接做互動圖。
- 用教材簡報頁截圖替代。
- 以「概念對應的簡報頁」取代「資訊圖」。

## 第 3 階段：建立駕駛艙初版

### 3.1 必要檔案

每個單元資料夾至少包含：

```text
index.html
style.css
script.js
assets/
```

### 3.2 建議頁面區塊

單元頁應包含：

- 頁首：科目、年級、單元、標題。
- 教學重點導覽。
- 學生自學影片。
- 互動視覺化。
- 形成性評量。
- 重點整理。
- 浮動工具：畫筆、工具箱。

### 3.3 教室工具

教師常用工具可包含：

- 抽籤。
- 計時器。
- 畫筆。
- 雷射筆。

工具設計原則：

- 不遮擋主要教學內容。
- 可在觸控螢幕操作。
- 操作按鈕要大。
- 不用大量文字說明，按鈕行為要直覺。

## 第 4 階段：互動與教室操作優化

這是最容易反覆修改的階段。要優先依照瀏覽器批註與實機預覽調整。

### 4.1 預覽方式

不要依賴 `file:///`。

原因：

- 中文路徑可能造成瀏覽器或 Google Drive 同步路徑問題。
- 部分資源在 file URL 下行為不同。

標準做法：

- 使用 localhost 預覽。
- 若目前資料夾是原始工作區，用該資料夾開 server。
- 若要測 GitHub 上傳版，用 `GitHub上傳版/` 開 server。

### 4.2 版面優化

本次表面積駕駛艙曾做過的優化：

- 從多欄版面改為單欄版面。
- 將五個教學重點放到頁面上方。
- 縮小標題、活動卡片、互動 SVG、表單元件高度。
- 讓一個活動盡量能在觸控螢幕一屏內看完整。

未來檢查：

- 桌機寬版。
- 教室觸控螢幕。
- 平板直向與橫向。
- 手機直向。

### 4.3 畫筆工具優化

本次最終規格：

- 畫筆面板要窄，不要像工具箱一樣寬。
- 點選顏色即代表開啟畫筆。
- 不需要「開啟畫筆」按鈕。
- 清除筆跡使用垃圾桶圖示。
- 畫筆粗細用滑桿。
- 雷射筆可以保留獨立開關。
- 雷射筆要有短暫殘影，方便學生追蹤。

### 4.4 計時器優化

本次最終規格：

- 常用時間：30 秒、1 分、2 分、5 分。
- 自訂時間放在「歸零」下方。
- 時間顏色要醒目。
- 歸零要同時停止倒數。
- 時間到不要顯示文字。
- 時間到使用圖案加 3 秒鈴聲。

### 4.5 互動視覺化優化

本次表面積案例規格：

- 長方體的長、寬、高控制垂直顯示。
- 正方體與長方體旋轉角度要清楚。
- 切開後新增表面用紅色標示。
- 堆疊後隱藏接觸面用紅色標示。
- 即時結果要同步更新。

## 第 5 階段：形成性評量規格

形成性評量要簡短、即時、低負擔。

### 5.1 題數

每個教學重點放 1-3 題。

目前可採：

- 初版：每個教學重點 1 題。
- 加強版：每個教學重點 2-3 題。

不要一次放太多題，避免駕駛艙變成測驗卷。

### 5.2 認知層次

以記憶、理解為主。

適合題型：

- 定義辨識。
- 概念判斷。
- 圖像對應。
- 簡單算式。
- 常見迷思選擇。

避免：

- 過度複雜的多步驟計算。
- 需要大量文字閱讀的題幹。
- 與本活動無關的延伸難題。

### 5.3 回饋

回饋應：

- 簡短。
- 直接指出概念。
- 不責備學生。
- 引導回上方圖像或操作。

範例：

```text
對。長方體有 6 個外面，要全部納入表面積。
```

### 5.4 技術結構

可在 `script.js` 中用資料結構管理題目。

單題版本：

```js
quiz: {
  q: "什麼是表面積？",
  options: ["立體形體所有面的面積總和", "立體形體裡面能裝多少", "立體形體最長邊的長度"],
  answer: 0,
  feedback: "對，表面積就是所有外面的面積加起來。"
}
```

若未來改成 1-3 題，可改為：

```js
quizzes: [
  {
    q: "什麼是表面積？",
    options: ["立體形體所有面的面積總和", "立體形體裡面能裝多少", "立體形體最長邊的長度"],
    answer: 0,
    feedback: "對，表面積就是所有外面的面積加起來。"
  }
]
```

## 第 6 階段：平板與手機響應式設計

學生會用平板或手機讀取駕駛艙內容，所以響應式不是加分項，而是必要條件。

### 6.1 基本設定

HTML 必須有：

```html
<meta name="viewport" content="width=device-width, initial-scale=1">
```

CSS 應使用：

- `clamp()` 控制文字大小。
- `width: min(...)` 限制最大寬度。
- `grid-template-columns` 搭配 media query。
- `max-width: 100%` 避免圖片溢出。
- `aspect-ratio` 保持影片比例。

### 6.2 斷點檢查

至少檢查：

- 390px 手機寬度。
- 768px 平板寬度。
- 1024px 平板橫向。
- 1366px 教室筆電。
- 1920px 大螢幕。

### 6.3 手機檢查清單

手機版要確認：

- 上方教學重點按鈕不重疊。
- 文字不超出卡片。
- 影片維持比例。
- 互動圖不超出畫面。
- 形成性評量選項容易點選。
- 畫筆與工具箱按鈕不遮住主要閱讀內容。
- 抽屜面板寬度不超出螢幕。

### 6.4 平板檢查清單

平板版要確認：

- 主要內容適合閱讀，不過度放大。
- 互動 SVG 可操作。
- 拉桿、按鈕足夠大。
- 工具箱在橫向與直向都能使用。
- 學生不需要縮放頁面才能完成閱讀。

## 第 7 階段：GitHub 上傳版整理

### 7.1 建立上傳版

所有要推上 GitHub 的內容放在：

```text
GitHub上傳版/
```

不要直接推原始教材工作區。

### 7.2 複製必要檔案

只複製：

- HTML。
- CSS。
- JS。
- 實際被引用的圖片。
- 實際被引用的 SVG。
- README 或 workflow 文件。
- `.nojekyll`。

### 7.3 排除檔案

不要複製：

- PDF。
- PPT/PPTX。
- 影片。
- 測試圖。
- 預覽圖。
- NotebookLM 原始輸出大檔。
- 下載說明之外的大型中間檔。

### 7.4 GitHub 限制檢查

發布前檢查：

- 單檔是否超過 100MB。
- 單檔是否超過 50MB。
- 整站是否遠低於 1GB。
- 是否包含影片、PDF、PPT。
- 是否有未使用的大圖。

本次標準：

- GitHub 上傳版總大小約 25.48MB。
- 最大單檔約 2.22MB。
- 沒有 PDF、影片、PPT。

## 第 8 階段：網站首頁與分類管理

目前網站採三層：

```text
總首頁
  國小數學首頁
    康軒五下第七單元-表面積
```

### 8.1 總首頁

路徑：

```text
GitHub上傳版/index.html
```

用途：

- 管理所有科目入口。
- 目前有「國小數學教學駕駛艙」。

### 8.2 科目首頁

路徑範例：

```text
GitHub上傳版/國小數學/index.html
```

用途：

- 管理該科目所有單元駕駛艙。
- 新增單元時，在這裡新增卡片。

### 8.3 單元頁

路徑範例：

```text
GitHub上傳版/康軒五下第七單元-表面積/index.html
```

要求：

- 單元頁要有「首頁」按鈕。
- 此按鈕回到科目首頁，不是總首頁。

範例：

```html
<a class="home-link" href="../國小數學/" aria-label="回國小數學首頁">首頁</a>
```

## 第 9 階段：本機驗證

### 9.1 語法檢查

至少檢查 JS：

```powershell
node --check path\to\script.js
```

### 9.2 localhost 檢查

用本機伺服器檢查：

- 總首頁。
- 科目首頁。
- 單元頁。

確認：

- HTTP 200。
- 首頁卡片可點。
- 科目首頁卡片可點。
- 單元頁可以回科目首頁。
- 主要圖片可載入。
- YouTube iframe 存在。

### 9.3 視覺檢查

至少檢查：

- 桌機寬版。
- 平板。
- 手機。
- 教室觸控螢幕若可取得，需實機測試。

## 第 10 階段：GitHub 發布

### 10.1 倉庫

目前統一管理倉庫：

```text
darma0406/teaching-dashboards
```

公開網址：

```text
https://darma0406.github.io/teaching-dashboards/
```

### 10.2 提交

在 `GitHub上傳版/` 裡提交，不要在外層教材工作區直接推送原始教材。

基本流程：

```powershell
git status --short --branch
git add <relevant files>
git commit -m "Add or update dashboard"
git push
```

### 10.3 部署

推送後等待 GitHub Pages 部署。

檢查：

- Pages workflow 是否成功。
- 公開首頁是否更新。
- 科目首頁是否更新。
- 單元頁是否正常。

### 10.4 公開使用

倉庫必須保持公開，讓所有學生與老師可使用。

若使用 YouTube 影片：

- 影片要公開或不公開但可嵌入。
- 不要使用需要登入才能看的影片連結。

## 第 11 階段：收工紀錄

完成後寫入 Obsidian：

- 每日筆記。
- 專案筆記 `Projects/02_114教材.md`。

紀錄包含：

- 完成的單元。
- GitHub 倉庫。
- GitHub Pages 網址。
- 最新提交。
- 未完成事項。
- 下次新增單元時要先做的事。

## 新增下一個駕駛艙的固定流程

每次新增同類型駕駛艙時，照以下順序：

1. 掃描教材與素材。
2. Codex 讀教材。
3. Claude 讀教材。
4. 彙整 3-5 個教學重點。
5. 產生或整理 NotebookLM 素材。
6. 建立原始單元資料夾。
7. 做出 `index.html`、`style.css`、`script.js`。
8. 建立 1-3 題記憶/理解層次形成性評量。
9. 做手機、平板、桌機響應式。
10. 用 localhost 預覽並依批註修正。
11. 複製精簡檔到 `GitHub上傳版/`。
12. 檢查 GitHub 檔案限制。
13. 更新科目首頁卡片。
14. 確認單元頁有回科目首頁按鈕。
15. 提交、推送、等待 Pages 部署。
16. 打開公開網址驗證。
17. 寫入 Obsidian 收工紀錄。

## 本次案例的最終公開網址

- 總首頁：https://darma0406.github.io/teaching-dashboards/
- 國小數學首頁：https://darma0406.github.io/teaching-dashboards/國小數學/
- 第七單元：https://darma0406.github.io/teaching-dashboards/康軒五下第七單元-表面積/
